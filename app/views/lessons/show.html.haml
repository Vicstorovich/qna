= link_to "Back", course_lessons_path, class: 'btn btn-success'

%h1= @lesson.title
%p= @lesson.description
%p= @lesson.conspectus
%p= image_tag @lesson.image.thumb.url
%p= @lesson.homework
%p= video_tag @lesson.video_url.to_s, :controls =>true

%h3 Send your homework
- if @lesson.homeworks.last.present?
  = @lesson.homeworks.last.body

= simple_form_for [@lesson, @lesson.homeworks.new], url: course_lesson_homeworks_path(course, @lesson), html: {method: :post} do |f|
  = f.input :body

  = f.submit 'Send'
